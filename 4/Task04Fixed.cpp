//FIXME Некорректный формат формулировки задания;

/* Строки, определяющие выражения, не содержат пробелов.
 * При выполнении заданий не следует использовать оператор цикла.
 * Вывести значение целочисленного выражения, заданного в виде строки S.
 * Выражение определяется следующим образом:
 * <выражение> :: = <цифра> | <выражение> +<цифра> | <выражение> − <цифра>
 */

#include "Task04.h"

int main()
{
    //FIXME Установа локали запрещена
	//setlocale(LC_ALL, "Russian");

    cout << "Программа считает значение выражения которое вы введете" << endl;
    string input;
    cout << "Введите выражение: ";
    getline(cin, input);

    //FIXME Добавлен блок try-catch для валидации ввода
    try {
        if (input.empty()) {
            throw invalid_argument("Пустой ввод");
        }
        //FIXME Создания дополнительной переменной можно избежать
        //int r = EvaluateExpression(input);
        //cout << "Ответ: " << r << endl;
        cout << "Ответ: " << EvaluateExpression(input) << endl;
    } catch (const exception& e) {
        cerr << "Ошибка: " << e.what() << endl;
        return 1;
    }

	return 0;
}




